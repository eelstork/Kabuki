⊐ Active.Core; ⊐̥ Active.Core.status;
⊐ UnityEngine;
⊐ Activ.Kabuki; ⊐̥ Activ.Kabuki.VectorExt;

‒ ○ Roam : Actor{

    ‒ ㅇ anchored = ✓;
    ‒ シ origin   = シ.zero;
    ‒ エ giz;

    シ? target;

    ⁺‒ ⑂ Step(){
        ⤴ (target.HasValue) ⮐ (~ Reach(target))ʾ ∧ Do( target = ∅ );
        ⤵ {
            target = Target();
            giz˙ = targetᖾ; ☡̱
        }
    }

    メ Target ⎚ RandomX_Z(15f) + (anchored ? origin : ⦿˙);

}
