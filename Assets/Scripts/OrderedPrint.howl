⊐ Active.Core; ⊐̥ Active.Core.status;
⊐ UnityEngine;

‒ ○ OrderedPrint : UTask{

    ⁺‒ ⑂ Step() → Sequence()[
        and ? Print("Step 1") :
        and ? Print("Step 2") :
        and ? Print("Step 3") : loop
    ];

    ⑂ Print(ㄹ arg){
        Debug.Log(arg); ◇̠
    }

}
