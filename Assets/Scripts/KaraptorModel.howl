⊐ UnityEngine;
⊐ Active.Core; ⊐̥ Active.Status;
⊐ Store = Kabuki.Store;

‒ ○ KaraptorModel : MonoBehaviour{

    ‒ Store nutrition = 0.5f;
    ‒ Store hydration = 0.5f;
    ‒ Store anger     = 0.0f;
    ‒ Store damage    = 0.0f;
    ㅅ irritability = 0.2f;

    ‒ ㅇ angry   → anger ≥ 1f;
    ‒ ㅇ hungry  → nutrition.want;
    ‒ ㅇ thirsty → hydration.want;
    ‒ ㅇ wounded → damage > 0.5f;

    ┈ Start(){
        damage.enabled = ✗;
    }

    ┈ Update(){
        anger.Update(AngerStimulus());
        nutrition.Update();
        hydration.Update();
        damage.Update();
    }

    ㅅ AngerStimulus(){
        ⤴ (ap.threat ☰ ∅) ⮐ 0;
        ㅅ d = シ.Distance(ap.threat˙, み˙);
        ⮐ (d < 3f) ? 1f
         : (d < 10f) ? irritability ᐧ 𝛿𝚝
         : 0f;
    }

    ┈ OnStrike(){
        damage.amount += 0.2f;
    }

    KaraptorAp ap → ⧼KaraptorAp⧽;

}
