⊐ NUnit.Framework;
⊐ Active.Core;

⊓ Kabuki.Test{
‒ ○ TieTest: EditorTest{

    ؟ And([Values(-1, 0, 1)]ᆞ x, [Values(-1, 0, 1)]ᆞ y){
        o( ⌢ tie(x) ∧ ⌢ tie(y), ⌢ tie(x));
    }

    ؟ Short([Values(-1, 1)]ᆞ x){
        ∙ a = 0;
        ∙ z = ⌢ tie(x) ∧ Do(a = 1);
        o(a, 0);
    }

    ؟ Bridge(){
        ∙ a = 0;
        ∙ x = tie.cont ∧ Do(a = 1);
        o(a, 1);
    }

    tie Do(⊡ arg) → tie.done;

}}
